# Compat Benchmarks

This directory contains the legacy compatibility backend adapter (`NBSymEngineLegacyAdapter`) used for benchmarking downstream compat behavior in `ci-03`.

Top-level benchmarks in the `benchmarks/` directory at the project root are direct-only and must not import `nbsymengine_compat`.
